import 'package:flutter/material.dart';

class DrawerFactos extends StatelessWidget {
  const DrawerFactos({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return IconButton(onPressed: () {}, icon: const Icon(Icons.menu));
  }
}
